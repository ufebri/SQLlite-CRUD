<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bedboy.crudsqlite">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.brand.AddBrandActivity"/>
        <activity android:name=".ui.customer.AddCustomerActivity" />
        <activity android:name=".ui.pembelian.AddPembelianActivity" />
        <activity android:name=".ui.product.AddProductActivity" />
        <activity android:name=".ui.brand.BrandActivity" />
        <activity android:name=".ui.customer.CustomerActivity" />
        <activity android:name=".ui.pembelian.PembelianActivity" />
        <activity android:name=".ui.product.ProductActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>